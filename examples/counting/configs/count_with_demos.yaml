name: test_count_with_demos
wandb_enabled: false
loader:
  source: data/items.json
  kwargs:
    image_dir: data/images
models:
  gemma:
    cls: openai
    base_url: "http://0.0.0.0:8000/v1"
    token: "-"
    model_id: SmolVLM-500M-Instruct
demo_pools:
  count_pool:
    source: data/demo_pool.json
    image_dir: data/images
stages:
  - inputs:
      - [Image, human]
    output: CountOutput
    processor: LLMProcessor
    model: gemma
    demo:
      pool: count_pool
      select: random
      shots: 2
